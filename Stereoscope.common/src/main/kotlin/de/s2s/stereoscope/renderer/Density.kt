package de.s2s.stereoscope.renderer

/**
 * Class to hold a display density
 *
 * @param xPPMM Horizontal resolution in pixel per mm
 * @param yPPMM Vertical resolution in pixel per mm
 */
data class Density(val xPPMM: Double, val yPPMM: Double)